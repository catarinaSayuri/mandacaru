<!DOCTYPE html>
<!-- Declaração do tipo de documento HTML5 -->
<html lang="pt-br">
<!-- Define o idioma da página como português do Brasil -->

<head>
  <!-- Início da seção de metadados do documento -->
  <meta charset="UTF-8" />
  <!-- Define a codificação de caracteres como UTF-8 -->
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <!-- Configura a viewport para responsividade em dispositivos móveis -->
       <link rel="icon" href="/assets/icon_title.png" type="image/x-icon">
  <title>Sobre nós | Mandacaru</title>
  <!-- Define o título da página exibido na aba do navegador -->
  <link rel="stylesheet" href="/css/sobreNosStyle.css" />
  <!-- Inclui o arquivo CSS para estilização específica da página Sobre Nós -->
  <link rel="stylesheet" href="/css/header.css" />
  <!-- Inclui o arquivo CSS para estilização do cabeçalho -->

  <!-- Importação de fontes: Poppins e Mulish -->
  <link rel="preconnect" href="https://fonts.googleapis.com" />
  <!-- Estabelece uma conexão prévia com o servidor de fontes do Google -->
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin />
  <!-- Estabelece uma conexão prévia com o servidor de fontes estáticas do Google, com suporte a CORS -->
  <link
    href="https://fonts.googleapis.com/css2?family=Mulish:ital,wght@0,200..1000;1,200..1000&family=Poppins:ital,wght@0,100;0,200;0,300;0,400;0,500;0,600;0,700;0,800;0,900;1,100;1,200;1,300;1,400;1,500;1,600;1,700;1,800;1,900&display=swap"
    rel="stylesheet" />
  <!-- Inclui as fontes Poppins (múltiplos pesos e estilos) e Mulish (múltiplos pesos e estilos) do Google Fonts -->
</head>

<body >
  <!-- Início do corpo do documento com a classe 'fade-in' para animação -->
  <header>
    <!-- Seção do cabeçalho da página -->
     
    <nav>
      <!-- Elemento de navegação -->
      <div>
        <!-- Contêiner para o logotipo -->
        <a href="/">
          <!-- Link para a página inicial -->
          <img class="logo" src="/assets/logo_mandacaru.svg" alt="logo_mandacaru" />
          <!-- Imagem do logotipo com classe 'logo' e descrição para acessibilidade -->
        </a>
      </div>
      <ul class="navbar">
        <!-- Lista de navegação para o menu principal -->
        <li><a href="/">Home</a></li>
        <!-- Item de menu com link para a página inicial -->
        <li><a href="/cultura">Cultura</a></li>
        <!-- Item de menu com link para a página de Cultura -->
        <li><a href="/esporte">Esporte</a></li>
        <!-- Item de menu com link para a página de Esporte -->
        <li><a href="/saude">Saúde Mental</a></li>
        <!-- Item de menu com link para a página de Saúde Mental -->
        <li class="active"><a href="/sobre">Sobre Nós</a></li>
        <!-- Item de menu ativo com link para a página Sobre Nós -->
      </ul>
      <button class="mobile-btn">
        <!-- Botão para exibir o menu em dispositivos móveis -->
        <img src="../assets/NavBar.svg" alt="" />
        <!-- Ícone do menu para dispositivos móveis -->
      </button>
    </nav>

    <div id="mobile-menu">
      <!-- Contêiner do menu móvel -->
      <ul class="mobile-navbar">
        <!-- Lista de navegação para o menu móvel -->
        <li><a href="/">Home</a></li>
        <!-- Item de menu móvel com link para a página inicial -->
        <li><a href="/cultura">Cultura</a></li>
        <!-- Item de menu móvel com link para a página de Cultura -->
        <li><a href="/esporte">Esporte</a></li>
        <!-- Item de menu móvel com link para a página de Esporte -->
        <li><a href="/saude">Saúde Mental</a></li>
        <!-- Item de menu móvel com link para a página de Saúde Mental -->
        <li><a href="/sobre">Sobre Nós</a></li>
        <!-- Item de menu móvel com link para a página Sobre Nós -->
      </ul>
    </div>
  </header>

    <main>
      <!-- Seção principal do conteúdo da página -->
      <div class="texto-principal">
        <!-- Contêiner para o texto principal -->
        <h1 class="texto-h1">
          Cultivando <span>Mentes</span>, <br />
         <span> Transformando </span>Futuros
        </h1>
        <!-- Título principal com palavras destacadas -->
        <p class="texto-subtitulo">
          Tenha conhecimento sobre tudo a sua volta!
        </p>
        <!-- Subtítulo motivacional -->
      </div>

            


    <section class="instituto">
      <!-- Seção sobre o instituto -->
      <h2>Sobre o instituto</h2>
      <!-- Título da seção -->
      <p class="texto-subtitulo">
        Transformamos vidas com cuidado, ciência e propósito.
      </p>
      <!-- Subtítulo explicativo -->
      <div class="cards">
        <!-- Contêiner para os cards -->
        <div class="card-1">
          <!-- Card sobre missão -->
          <img class="icone" src="../assets/sobreNos/icon1.svg" alt="Icone-1" />
          <!-- Ícone do card -->
          <h3>Missão</h3>
          <!-- Título do card -->
          <p>
            Desenvolvemos iniciativas que promovem saúde mental e educação,
            gerando impacto positivo e duradouro em comunidades por todo o
            Brasil.
          </p>
          <!-- Descrição da missão -->
          <span class="link-instituto">
            <a href="#missao">Saiba mais</a>
            <!-- Link para a seção de missão -->
            <img src="../assets/sobreNos/seta.svg" alt="" />
            <!-- Ícone de seta -->
          </span>
        </div>
        <div class="card-1">
          <!-- Card sobre projetos -->
          <img class="icone" src="../assets/sobreNos/icon2.svg" alt="Icone-1" />
          <!-- Ícone do card -->
          <h3>Projetos</h3>
          <!-- Título do card -->
          <p>
            Cultivar o potencial humano por meio do cuidado emocional e do
            acesso ao conhecimento, construindo uma sociedade mais acolhedora.
          </p>
          <!-- Descrição dos projetos -->
          <span class="link-instituto">
            <a href="#projeto">Saiba mais</a>
            <!-- Link para a seção de projetos -->
            <img src="../assets/sobreNos/seta.svg" alt="" />
            <!-- Ícone de seta -->
          </span>
        </div>
        <div class="card-1">
          <!-- Card sobre equipe -->
          <img class="icone" src="../assets/sobreNos/icon3.svg" alt="Icone-1" />
          <!-- Ícone do card -->
          <h3>Equipe</h3>
          <!-- Título do card -->
          <p>
            Uma equipe multidisciplinar unida pelo propósito de transformar
            vidas com empatia, ciência e compromisso social.
          </p>
          <!-- Descrição da equipe -->
          <span class="link-instituto">
            <a href="#equipe">Saiba mais</a>
            <!-- Link para a seção de equipe -->
            <img src="../assets/sobreNos/seta.svg" alt="" />
            <!-- Ícone de seta -->
          </span>
        </div>
      </div>
    </section>

    <div class="tela-container-principal">

    <section class="instituto-missao" id="missao">
      <!-- Seção sobre a missão -->
      <h2>Nossa Missão</h2>
      <!-- Título da seção -->
      <p class="texto-subtitulo">Cuidar de Vidas, Transformar Realidades</p>
      <!-- Subtítulo explicativo -->
      <div class="conteudo-missao">
        <!-- Contêiner para o conteúdo da missão -->
        <img src="../assets/sobreNos/mascote.png" alt="" />
        <!-- Imagem do mascote -->
        <p>
          A missão do Instituto Cactus é promover o cuidado com a saúde mental
          de forma ampla, acessível e livre de estigmas. Por meio de
          iniciativas que unem educação, cultura, tecnologia e articulação
          social, buscamos ampliar o acesso à informação, fortalecer redes de
          apoio e transformar a forma como a saúde emocional é percebida e
          vivida no Brasil.
        </p>
        <!-- Descrição detalhada da missão -->
      </div>
    </section>

    <section class="instituto-projetos">
      <!-- Seção sobre os projetos -->
      <h2 id="projeto">Nossos Projetos</h2>
      <!-- Título da seção -->
      <p class="texto-subtitulo">Iniciativas que fazem a diferença</p>
      <!-- Subtítulo explicativo -->
      <div class="projetos-navegacao">
        <!-- Contêiner para navegação dos projetos -->
        <img src="../assets/sobreNos/seta_esquerda.svg" id="esquerda" alt="seta_esquerda" class="btn-navegacao" />
        <!-- Botão de navegação para esquerda -->
        <div class="conteudo-projetos" id="projeto-cards-2"></div>
        <!-- Contêiner para os cards de projetos (desktop) -->
        <div class="conteudo-projetos" id="projeto-cards-2-mobile"></div>
        <!-- Contêiner para os cards de projetos (mobile) -->
        <img src="../assets/sobreNos/seta_direita.svg" id="direita" alt="seta_direita" class="btn-navegacao" />
        <!-- Botão de navegação para direita -->
      </div>
    </section>

    <section class="instituto-desenvolvedores">
      <h2 id="desenvolvedores">Nossos Desenvolvedores</h2>
      <p class="texto-subtitulo">
        Estudantes apaixonados por tecnologia e inovação
      </p>
      <div class="conteudo-desenvolvedores" id="desenvolvedores-cards"></div>
    </section>

    <section class="instituto-equipe">
      <!-- Seção sobre a equipe -->
      <h2 id="equipe">Nossa Equipe</h2>
      <!-- Título da seção -->
      <p class="texto-subtitulo">
        Profissionais apaixonados por transformar vidas
      </p>
      <!-- Subtítulo explicativo -->
      <div class="conteudo-equipe" id="equipe-cards-3"></div>
      <!-- Contêiner para os cards da equipe -->
    </section>

    </div>
  </main>

  <footer>
    <!-- Rodapé do site -->
    <p>© 2025, projeto Mandacaru - Inteli</p>
    <!-- Texto de direitos autorais -->
    <div class="icones-rodape">
      <!-- Contêiner para ícones de redes sociais -->
      <a href="https://github.com/Inteli-College/2025-1B-T17-IN02-G01" class="link-icone-rodape">
        <!-- Link para o GitHub -->
        <img src="../assets/sobreNos/icon_github.svg" alt="" class="icone-rodape" />
        <!-- Ícone do GitHub -->
      </a>
      <a href="https://msha.ke/projeto_mandacaru/" class="link-icone-rodape">
        <!-- Link para o LinkedIn -->
        <img src="../assets/sobreNos/icon_linkedin.svg" alt="" class="icone-rodape" />
        <!-- Ícone do LinkedIn -->
      </a>
    </div>
  </footer>

  <button class="scroll-top" aria-label="Voltar ao topo">
    <!-- Botão para voltar ao topo da página -->
    <svg width="24" height="24" fill="none" stroke="#fff" stroke-width="2" stroke-linecap="round"
      stroke-linejoin="round">
      <!-- Ícone SVG de seta para cima -->
      <polyline points="18 15 12 9 6 15"></polyline>
      <!-- Define os pontos da seta -->
    </svg>
  </button>

  <script>
    // Início do script JavaScript

    const mobileBtn = document.querySelector(".mobile-btn");
    // Seleciona o botão do menu móvel
    const mobileMenu = document.getElementById("mobile-menu");
    // Seleciona o contêiner do menu móvel

    mobileBtn.addEventListener("click", () => {
      // Adiciona um ouvinte de evento de clique no botão do menu móvel
      mobileMenu.classList.toggle("active");
      // Alterna a classe 'active' para exibir ou ocultar o menu móvel
    });

    // Fecha o menu ao clicar em um item
    const mobileLinks = document.querySelectorAll(".mobile-navbar a");
    // Seleciona todos os links do menu móvel
    mobileLinks.forEach((link) => {
      // Itera sobre cada link
      link.addEventListener("click", () => {
        // Adiciona um ouvinte de evento de clique
        mobileMenu.classList.remove("active");
        // Remove a classe 'active' para fechar o menu móvel ao clicar em um link
      });
    });

    // Scroll to top button
    const scrollTopBtn = document.querySelector(".scroll-top");
    // Seleciona o botão de voltar ao topo
    const footer = document.querySelector("footer");
    // Seleciona o rodapé

    window.addEventListener("scroll", () => {
      // Adiciona um ouvinte de evento para o scroll da janela
      const scrollPosition = window.scrollY;
      // Obtém a posição atual do scroll
      const windowHeight = window.innerHeight;
      // Obtém a altura da janela
      const footerHeight = footer.offsetHeight;
      // Obtém a altura do rodapé
      const footerTop = footer.getBoundingClientRect().top + scrollPosition;
      // Calcula a posição do topo do rodapé
      const limitDistance = 80;
      // Define a distância mínima do botão ao rodapé

      if (scrollPosition > 300) {
        // Verifica se o usuário rolou mais de 300px
        scrollTopBtn.classList.add("visible");
        // Adiciona a classe 'visible' para mostrar o botão
      } else {
        scrollTopBtn.classList.remove("visible");
        // Remove a classe 'visible' para ocultar o botão
      }

      if (footerTop <= windowHeight) {
        // Verifica se o rodapé está visível na janela
        const newBottom = Math.max(limitDistance, footerHeight + 20);
        // Calcula a nova posição inferior do botão
        scrollTopBtn.style.bottom = `${newBottom}px`;
        // Ajusta a posição do botão para evitar sobreposição com o rodapé
      } else {
        scrollTopBtn.style.bottom = "20px";
        // Define a posição padrão do botão
      }
    });

    scrollTopBtn.addEventListener("click", () => {
      // Adiciona um ouvinte de evento de clique no botão de voltar ao topo
      window.scrollTo({ top: 0, behavior: "smooth" });
      // Rola a página suavemente até o topo
    });

    const projetos = [
      // Array com informações dos projetos
      {
        titulo: "Panorama da Saúde Mental",
        descricao:
          "Ferramenta dinâmica de pesquisa e monitoramento contínuo voltada à saúde mental.",
        imagem: "../assets/sobreNos/projeto_1.png",
      },
      {
        titulo: "Caminhos em Saúde Mental",
        descricao:
          "Revisão de literatura (Brasil+Mundo) para capacitar projetos de promoção de SM.",
        imagem: "../assets/sobreNos/projeto_2.png",
      },
      {
        titulo: "Painel da Saúde Mental",
        descricao:
          "Plataforma que fornece informações para tomada de decisão dos gestores públicos.",
        imagem: "../assets/sobreNos/projeto_3.png",
      },
      {
        titulo: "Mapa da Saúde Mental",
        descricao:
          "Ajuda gestores a identificar bairros que precisam de melhorias na saúde mental.",
        imagem: "../assets/sobreNos/projeto_4.png",
      },
    ];

    const cardsPorPagina = 4;
    // Número de cards exibidos por página no desktop
    const cardsPorPaginaMobile = 1;
    // Número de cards exibidos por página no mobile
    let indiceAtualDesktop = 0;
    // Índice atual para navegação no desktop
    let indiceAtualMobile = 0;
    // Índice atual para navegação no mobile

    const card2 = document.getElementById("projeto-cards-2");
    // Seleciona o contêiner dos cards de projetos (desktop)
    const btnEsquerda = document.getElementById("esquerda");
    // Seleciona o botão de navegação para esquerda
    const btnDireita = document.getElementById("direita");
    // Seleciona o botão de navegação para direita
    const card2Mobile = document.getElementById("projeto-cards-2-mobile");
    // Seleciona o contêiner dos cards de projetos (mobile)

    function isMobile() {
      // Função para verificar se a tela é mobile
      return window.innerWidth <= 468;
    }

    function renderizarCardsDesktop() {
      // Função para renderizar cards no desktop
      card2.innerHTML = "";
      // Limpa o contêiner

      const inicio = indiceAtualDesktop * cardsPorPagina;
      // Calcula o índice inicial
      const fim = inicio + cardsPorPagina;
      // Calcula o índice final
      const cardsVisiveis = projetos.slice(inicio, fim);
      // Obtém os projetos visíveis

      cardsVisiveis.forEach((proj) => {
        // Itera sobre os projetos visíveis
        const card = document.createElement("div");
        // Cria um novo elemento div
        card.classList.add("card-2");
        // Adiciona a classe 'card-2'
        card.innerHTML = `
                    <img src="${proj.imagem}" alt="${proj.titulo}">
                    <h3>${proj.titulo}</h3>
                    <p>${proj.descricao}</p>
                `;
        // Define o conteúdo do card
        card2.appendChild(card);
        // Adiciona o card ao contêiner
      });

      btnEsquerda.disabled = indiceAtualDesktop === 0;
      // Desativa o botão de esquerda se estiver na primeira página
      btnDireita.disabled = fim >= projetos.length;
      // Desativa o botão de direita se não houver mais projetos
    }

    function renderizarCardsMobile() {
      // Função para renderizar cards no mobile
      card2Mobile.innerHTML = "";
      // Limpa o contêiner

      const inicio = indiceAtualMobile * cardsPorPaginaMobile;
      // Calcula o índice inicial
      const fim = inicio + cardsPorPaginaMobile;
      // Calcula o índice final
      const cardsVisiveis = projetos.slice(inicio, fim);
      // Obtém os projetos visíveis

      cardsVisiveis.forEach((proj) => {
        // Itera sobre os projetos visíveis
        const card = document.createElement("div");
        // Cria um novo elemento div
        card.classList.add("card-2");
        // Adiciona a classe 'card-2'
        card.innerHTML = `
                    <img src="${proj.imagem}" alt="${proj.titulo}">
                    <h3>${proj.titulo}</h3>
                    <p>${proj.descricao}</p>
                `;
        // Define o conteúdo do card
        card2Mobile.appendChild(card);
        // Adiciona o card ao contêiner
      });

      btnEsquerda.disabled = indiceAtualMobile === 0;
      // Desativa o botão de esquerda se estiver na primeira página
      btnDireita.disabled = fim >= projetos.length;
      // Desativa o botão de direita se não houver mais projetos
    }

    function renderizarCards() {
      // Função para renderizar os cards com base no dispositivo
      if (isMobile()) {
        renderizarCardsMobile();
      } else {
        renderizarCardsDesktop();
      }
    }

    btnEsquerda.addEventListener("click", () => {
      // Adiciona um ouvinte de evento ao botão de esquerda
      if (isMobile()) {
        if (indiceAtualMobile > 0) {
          indiceAtualMobile--;
          renderizarCardsMobile();
        }
      } else {
        if (indiceAtualDesktop > 0) {
          indiceAtualDesktop--;
          renderizarCardsDesktop();
        }
      }
    });

    btnDireita.addEventListener("click", () => {
      // Adiciona um ouvinte de evento ao botão de direita
      if (isMobile()) {
        if ((indiceAtualMobile + 1) * cardsPorPaginaMobile < projetos.length) {
          indiceAtualMobile++;
          renderizarCardsMobile();
        }
      } else {
        if ((indiceAtualDesktop + 1) * cardsPorPagina < projetos.length) {
          indiceAtualDesktop++;
          renderizarCardsDesktop();
        }
      }
    });

    window.addEventListener("resize", renderizarCards);
    // Adiciona um ouvinte de evento para redimensionamento da janela
    renderizarCards();
    // Renderiza os cards inicialmente

    const equipe = [
      // Array com informações dos membros da equipe
      {
        nome: "Maria Fernanda Quartiero",
        cargo: "Fundadora & Diretora Presidente",
        imagem: "../assets/sobreNos/equipe_1.png",
        link: "https://www.linkedin.com/in/maria-fernanda-resende-quartiero-a949b91a3/?originalSubdomain=br",
      },
      {
        nome: "Bruno Ziller",
        cargo: "Gerente Executivo",
        imagem: "../assets/sobreNos/equipe_2.png",
        link: "https://www.linkedin.com/in/bruno-ziller-80853b52/",
      },
      {
        nome: "Bianca Kann",
        cargo: "Especialista em Saúde Mental",
        imagem: "../assets/sobreNos/equipe_3.png",
        link: "https://www.linkedin.com/in/bianca-kann-81aa2b147/",
      },
      {
        nome: "Tais Moriyama",
        cargo: "Consultora",
        imagem: "../assets/sobreNos/equipe_4.png",
        link: "",
      },
      {
        nome: "Bruno Pimentel",
        cargo: "Analista de Dados",
        imagem: "../assets/sobreNos/equipe_5.png",
        link: "https://www.linkedin.com/in/bruno-pimentel-/",
      },
      {
        nome: "David Medina",
        cargo: "Coordenador de Comunicação",
        imagem: "../assets/sobreNos/equipe_6.png",
        link: "https://www.linkedin.com/in/davidmedinajor/",
      },
    ];

    const conteudoEquipe = document.getElementById("equipe-cards-3");
    // Seleciona o contêiner dos cards da equipe

    equipe.forEach((membro) => {
      // Itera sobre cada membro da equipe
      const card = document.createElement("div");
      // Cria um novo elemento div
      card.classList.add("card-3");
      // Adiciona a classe 'card-3'
      card.innerHTML = `
              <a href="${membro.link}" class="equipe-cactus">
                <img src="${membro.imagem}" alt="${membro.nome}">
                <h3>${membro.nome}</h3>
                <p>${membro.cargo}</p>
                </a>
            `;
      // Define o conteúdo do card
      conteudoEquipe.appendChild(card);
      // Adiciona o card ao contêiner
    });

    // Array com informações dos desenvolvedores
    const desenvolvedores = [
      {
        nome: "Catarina Sayuri",
        cargo: "Engenharia de Software",
        linkedin: "https://www.linkedin.com/in/catarina-sayuri/",
        imagem: "../assets/sobreNos/catarina.png", // Placeholder padrão
      },
      {
        nome: "Giovanna Neves",
        cargo: "Sistemas de Informação",
        linkedin: "https://www.linkedin.com/in/giovanna-neves-rodrigues-6927262b8/",
        imagem: "../assets/sobreNos/giovanna.png", // Placeholder padrão
      },
      {
        nome: "Isabel Montenegro",
        cargo: "Engenharia de Software",
        linkedin: "https://www.linkedin.com/in/isabel-montenegro-133b5a345/",
        imagem: "../assets/sobreNos/isabel.png", // Placeholder padrão
      },
      {
        nome: "Lavínia Pietra",
        cargo: "Administração e Tecnologia",
        linkedin: "https://www.linkedin.com/in/lav%C3%ADnia-mendon%C3%A7a-b4b977243/",
        imagem: "../assets/sobreNos/lavinia.png", // Placeholder padrão
      },
      {
        nome: "Lorena Gabriela",
        cargo: "Administração e Tecnologia",
        linkedin: "https://www.linkedin.com/in/llorengarcia/",
        imagem: "../assets/sobreNos/lorena.png", // Placeholder padrão
      },
      {
        nome: "Nicole Riedla",
        cargo: "Engenharia de Computação",
        linkedin: "https://www.linkedin.com/in/nicoleriedla/",
        imagem: "../assets/sobreNos/nicole.png", // Placeholder padrão
      },
      {
        nome: "Vitor Lucena",
        cargo: "Administração e Tecnologia",
        linkedin: "https://www.linkedin.com/in/vitor-lopes-91763b34a/",
        imagem: "../assets/sobreNos/vitor.png", // Placeholder padrão
      },
    ];

    // Seleciona o contêiner dos cards dos desenvolvedores
    const conteudoDesenvolvedores = document.getElementById("desenvolvedores-cards");

    // Renderiza os cards dos desenvolvedores
    desenvolvedores.forEach((dev) => {
      const card = document.createElement("div");
      card.classList.add("card-dev");
      card.innerHTML = `
    <a href="${dev.linkedin}" target="_blank" rel="noopener noreferrer">
      <img src="${dev.imagem}" alt="${dev.nome}">
      <h3>${dev.nome}</h3>
      <p>${dev.cargo}</p>
    </a>
  `;
      conteudoDesenvolvedores.appendChild(card);
    });
  </script>
  <script src="https://cdn.userway.org/widget.js" data-account="YswjRxBH1z"></script>
  <!-- Inclui o script do UserWay para acessibilidade -->
</body>

</html>